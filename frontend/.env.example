# StudIQ Frontend Production Environment Template (frontend/.env.example)
# Copy to frontend/.env.production before building production bundle: npm run build

# Base API URL pointing to the deployed FastAPI production backend
VITE_API_URL=https://studiq-api.yourdomain.com/api/v1
